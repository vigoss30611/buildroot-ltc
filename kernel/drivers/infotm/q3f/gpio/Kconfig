#
# GPIO system
#
#

menuconfig INFOTM_GPIO_CHIP
  bool "Infotm gpio chip driver support"
  help
    Infotm gpio chip driver support

if INFOTM_GPIO_CHIP

config Q3F_GPIO_CHIP
 bool "Q3F gpio chip driver support"
 help
   q3f gpio chip driver support

endif
