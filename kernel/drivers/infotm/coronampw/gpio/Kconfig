#
# GPIO system
#
#

menuconfig INFOTM_GPIO_CHIP
  bool "Infotm gpio chip driver support"
  help
    Infotm gpio chip driver support

if INFOTM_GPIO_CHIP

config CORONAMPW_GPIO_CHIP
 bool "CORONAMPW gpio chip driver support"
 help
   coronampw gpio chip driver support

endif
