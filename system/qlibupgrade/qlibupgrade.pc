prefix=/usr
exec_prefix=/usr
libdir=${exec_prefix}/lib
includedir=${prefix}/include/qsdk

Name: upgrade
Description: libupgrade  infotm
Version: 1.0.0
Requires: 
Libs: -L${libdir} -lupgrade 
Libs.private: -lm -ldl -lpthread -lrt -lsys
# -I${includedir}/alsa below is just for backward compatibility
# (it was set so mistakely in the older version)
Cflags:  -I${includedir}
