<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <ImageView
        android:id="@+id/device_icon"
        android:layout_width="@dimen/wifi_icon_size"
        android:layout_height="@dimen/wifi_icon_size"
        android:layout_alignParentLeft="true"
        android:padding="@dimen/padding_default"
        android:src="@drawable/wifi_device_icon" />

    <TextView
        android:id="@+id/device_name"
        android:layout_width="wrap_content"
        android:layout_height="40dip"
        android:layout_toRightOf="@id/device_icon"
        android:gravity="bottom"
        android:padding="@dimen/padding_default"
        android:textSize="21sp" />

    <ImageView
        android:id="@+id/device_lev"
        android:layout_width="20dip"
        android:layout_height="20dip"
        android:layout_alignBottom="@+id/device_icon"
        android:layout_toRightOf="@+id/device_icon"
        android:padding="@dimen/padding_default"
        android:src="@drawable/danale_cloud_device_list_offline" />

</RelativeLayout>