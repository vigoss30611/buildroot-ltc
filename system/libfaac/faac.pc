prefix=/usr
exec_prefix=/usr
libdir=${exec_prefix}/lib
includedir=${prefix}/include

Name:  faac
Description: libfaac  infotm
Version: 1.28
Requires: 
Libs: -L${libdir} -lfaac
Libs.private: -lm -ldl -lpthread -lrt
# -I${includedir}/faac below is just for backward compatibility
# (it was set so mistakely in the older version)
Cflags:  -I${includedir}/faac
